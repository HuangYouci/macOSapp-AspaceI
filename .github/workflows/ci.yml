name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: 建置
        run: swift build
      - name: 測試
        run: swift test
      - name: 驗證 App bundle
        run: ./scripts/build-app.sh
